#!/usr/bin/env bash
# This script displays all active IPv4 IP addresses on the machine.

# Use `ip` command to list IP addresses, filter for IPv4 addresses, and format output.
ip -4 addr show | awk '/inet/ {print $2}' | cut -d/ -f1
